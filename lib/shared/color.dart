// Default color when the field is inactive (not clicked)
import 'dart:ui';

import 'package:flutter/material.dart';

const Color defaultInactiveColor = Colors.grey;
// Default color when the field is active (clicked)
const Color defaultActiveColor = Colors.redAccent;